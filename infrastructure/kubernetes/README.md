# infrastructure/kubernetes

Reserved. Phase 1's deployment target is local/self-hosted `docker-compose` only (see
`docs/adr/0001-phase-1-stack.md`). Kubernetes manifests are out of scope until this platform actually
needs cloud/horizontal-scaling deployment (spec section 42), which is explicitly not a Phase 1
concern.
