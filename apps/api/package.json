{
  "name": "@maddox-bot/api",
  "version": "0.1.0",
  "private": true,
  "description": "Fastify REST + WebSocket gateway: webhook receivers, session/task CRUD, real-time push to the VS Code extension.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@fastify/websocket": "^11.0.2",
    "@maddox-bot/agent-core": "workspace:*",
    "@maddox-bot/database": "workspace:*",
    "@maddox-bot/events": "workspace:*",
    "@maddox-bot/github": "workspace:*",
    "@maddox-bot/jira": "workspace:*",
    "@maddox-bot/queue": "workspace:*",
    "@maddox-bot/shared": "workspace:*",
    "fastify": "^5.2.0",
    "fastify-raw-body": "^6.0.1"
  },
  "devDependencies": {
    "@prisma/client": "^6.19.3",
    "@types/ws": "^8.5.13",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "ws": "^8.18.0"
  }
}
