# @maddox-bot/sandbox

Docker sandbox lifecycle: create/clone/exec-with-limits/destroy, via the worker's mounted Docker socket.

> Scaffolded in increment 1. Real implementation lands in the increment named for this package in
> the approved plan (`~/.claude/plans/lazy-gathering-spark.md`, section 8).
