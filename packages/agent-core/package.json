{
  "name": "@maddox-bot/agent-core",
  "version": "0.1.0",
  "private": true,
  "description": "TaskStateMachine, ContextBuilder, and AgentLoopRunner — the orchestration layer driving the Planner and Implementation Agent roles over one shared tool-use loop.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@maddox-bot/agent-tools": "workspace:*",
    "@maddox-bot/database": "workspace:*",
    "@maddox-bot/llm": "workspace:*",
    "@maddox-bot/permissions": "workspace:*",
    "@maddox-bot/shared": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@maddox-bot/git": "workspace:*",
    "@vitest/coverage-v8": "^2.1.8",
    "simple-git": "^3.27.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
