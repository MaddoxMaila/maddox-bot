# tests/fixtures

Sample target repository (a minimal Node/TS project the sandbox can clone, install, test, and open a
real PR against) and sample GitHub/Jira webhook payloads used by the increment 5–8 and 17 tests.
Populated starting at increment 5 (webhook fixtures) and increment 8 (sample repo).
