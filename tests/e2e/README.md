# tests/e2e

The Phase 1 end-to-end scenario (increment 17): a fixture Jira issue transitioned to `AI READY`
drives the full pipeline through to an opened GitHub PR, asserting every intermediate task-state
transition. Empty until increment 17.
